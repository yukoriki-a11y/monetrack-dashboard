# ダッシュボードを人に渡す手順

社内向け。**URL を渡すだけでは見られません**。先に Supabase でユーザーを作る必要があります。

## 渡す相手に必要なもの

| 渡すもの | 中身 |
| --- | --- |
| URL | https://yukoriki-a11y.github.io/monetrack-dashboard/ |
| ログイン | 招待メールから相手が自分で設定したパスワード |

接続先（Project URL と公開用APIキー）は `js/config.js` に入れてあるので、
相手が入力する必要はありません。**開いてログインするだけ**です。

## 1. ユーザーを作る

新規登録は止めてあります（勝手にアカウントを作られないため）。管理者が作ります。

1. Supabase → **Authentication** → **Users**
2. **Add user** → **Send invitation email**
3. 相手のメールアドレスを入れて送る

**「Create new user」でパスワードを直接入れないでください。**
招待メールなら相手が自分でパスワードを決めるので、他人のパスワードを
こちらで持たずに済みます。

## 2. URL を渡す

それだけです。相手は招待メールでパスワードを設定してから、URL を開いて
ログインします。

## いま渡すと、その人にできること

RLS のポリシーは「ログイン済みなら全部OK」なので、**ログインした人は
全員が同じものを見られ、同じことができます**。

- 全期間・全広告主・全アフィリエイターのデータ閲覧
- CSV の書き出し
- **データの取り込み（上書きを含む）**

閲覧だけの人を作りたい、広告主ごとに見える範囲を分けたい、という場合は
作り込みが必要です。

## 抜けた人を止めるとき

Supabase → **Authentication** → **Users** → その行の **⋯** → **Delete user**
（または **Ban user**）。次のアクセスから入れなくなります。

## なぜ URL が公開でも平気なのか

このリポジトリは公開で、`js/config.js` の anon key も誰でも読めます。
それでもデータが見えないのは、二段で止めているためです。

1. テーブルは全部 RLS が有効で、ポリシーは `authenticated` 向けだけ
2. `public` の関数は anon から実行権を剥がしてある（`sql/013_lock_anon.sql`）

実データで次を確認済みです。

| 確認項目 | 結果 |
| --- | --- |
| RLS が付いていないテーブル | なし |
| anon が読めるテーブル | なし |
| anon が実行できる関数 | なし |
| anon 向けのポリシー | なし |

つまり anon key だけでは 1 件も読めません。読むにはログインが要ります。

**`service_role` / `sb_secret_` のキーは `js/config.js` に絶対に書かないこと。**
あれは RLS を無視できる管理者キーで、書いた時点で全データが公開になります。
（画面側でも、その2種類は貼られても弾くようにしてあります）
