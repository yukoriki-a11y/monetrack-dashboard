// 接続先をチーム全員に配りたい場合だけ、ここに書き込む。
//
// 空のままにしておくと、初回アクセス時に画面上で入力する形になり、
// 入力値はそのブラウザの localStorage にだけ保存される（リポジトリには入らない）。
//
// ここに書いた場合は、リポジトリを見られる人全員に anon key が見えることになる。
// anon key は「公開前提」の鍵で、実データは RLS（ログイン必須）で守られているが、
// 公開リポジトリに置きたくなければ空のままにしておくのが安全。

export const DEFAULT_SUPABASE_URL = '';
export const DEFAULT_SUPABASE_ANON_KEY = '';
